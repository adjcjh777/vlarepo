interface:
  display_name: "Agent Bus Register"
  short_description: "Register this session and claim Bus tasks"
  default_prompt: "Use $agent-bus-register to register this Codex session with Agent Bus and claim queued work."
