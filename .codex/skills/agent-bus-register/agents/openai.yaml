interface:
  display_name: "Agent Bus Register"
  short_description: "Register and claim queued Agent Bus tasks"
  default_prompt: "Use $agent-bus-register to register this Codex session and claim any queued Agent Bus tasks."
